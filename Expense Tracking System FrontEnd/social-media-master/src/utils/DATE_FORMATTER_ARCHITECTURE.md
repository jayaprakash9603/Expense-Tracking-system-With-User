# Date Formatter Architecture

## System Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                    User Settings System                          │
│  (Redux Store + useUserSettings Hook)                           │
│                                                                   │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ Settings Configuration                                    │   │
│  │ - DD/MM/YYYY (UK/EU)                                     │   │
│  │ - MM/DD/YYYY (US)                                        │   │
│  │ - YYYY-MM-DD (ISO)                                       │   │
│  │ + Future formats...                                       │   │
│  └─────────────────────────────────────────────────────────┘   │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                           │ User's dateFormat preference
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│               Date Formatter Utility                             │
│         (src/utils/dateFormatter.js)                            │
│                                                                   │
│  ┌────────────────────────────────────────────────────────┐    │
│  │ Token-Based Formatting Engine                          │    │
│  │                                                          │    │
│  │ Tokens: YYYY, YY, MM, M, DD, D                        │    │
│  │ Separators: /, -, ., (any character)                  │    │
│  │ Logic: Pattern replacement with validation            │    │
│  └────────────────────────────────────────────────────────┘    │
│                                                                   │
│  ┌────────────────┐  ┌────────────────┐  ┌────────────────┐   │
│  │  formatDate()  │  │formatDateTime()│  │formatRelative()│   │
│  │ Main function  │  │ With time      │  │ "Today", etc.  │   │
│  └────────────────┘  └────────────────┘  └────────────────┘   │
│                                                                   │
│  ┌────────────────┐  ┌────────────────┐  ┌────────────────┐   │
│  │  parseDate()   │  │getMonthName()  │  │  getDayName()  │   │
│  │ String to Date │  │ Localized      │  │  Localized     │   │
│  └────────────────┘  └────────────────┘  └────────────────┘   │
│                                                                   │
│  ┌────────────────┐  ┌────────────────┐  ┌────────────────┐   │
│  │isValidFormat() │  │ getSeparator() │  │getFormatExample│   │
│  │ Validation     │  │ Extract sep    │  │ Show example   │   │
│  └────────────────┘  └────────────────┘  └────────────────┘   │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                           │ Imported by components
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│                    React Components                              │
│                                                                   │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐         │
│  │ BillReport   │  │ BillTable    │  │ Analytics    │         │
│  │ ✅ Updated   │  │ ⏳ Pending   │  │ ⏳ Pending   │         │
│  └──────────────┘  └──────────────┘  └──────────────┘         │
│                                                                   │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐         │
│  │ NewExpense   │  │ EditExpense  │  │ CreateBill   │         │
│  │ ✅ Good      │  │ ✅ Good      │  │ ✅ Good      │         │
│  └──────────────┘  └──────────────┘  └──────────────┘         │
│                                                                   │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐         │
│  │ Charts       │  │ Notifications│  │ Export/PDF   │         │
│  │ ⏳ Pending   │  │ ⏳ Pending   │  │ ⏳ Pending   │         │
│  └──────────────┘  └──────────────┘  └──────────────┘         │
└─────────────────────────────────────────────────────────────────┘
```

---

## Data Flow Diagram

```
┌─────────┐
│  User   │
│ Changes │
│ Settings│
└────┬────┘
     │
     │ Updates dateFormat preference
     ▼
┌─────────────────┐
│ Redux Store     │
│ userSettings    │
│ dateFormat: "..." │
└────┬────────────┘
     │
     │ Accessed via hook
     ▼
┌─────────────────┐
│useUserSettings()│
│   Hook          │
└────┬────────────┘
     │
     │ settings.dateFormat
     ▼
┌─────────────────────────┐
│   React Component       │
│                         │
│ const settings =        │
│   useUserSettings();    │
│                         │
│ formatDate(date,        │
│   settings.dateFormat)  │
└────┬────────────────────┘
     │
     │ Calls utility function
     ▼
┌─────────────────────────────┐
│  dateFormatter.js           │
│                             │
│  formatDate(                │
│    "2024-12-31",           │
│    "DD/MM/YYYY"            │
│  )                          │
│                             │
│  1. Parse date             │
│  2. Extract components      │
│  3. Replace tokens          │
│  4. Return formatted string │
└────┬────────────────────────┘
     │
     │ Returns formatted date
     ▼
┌─────────────────┐
│   "31/12/2024"  │
│  Displayed to   │
│      User       │
└─────────────────┘
```

---

## Token Replacement Algorithm

```
Input: date="2024-12-31", format="DD/MM/YYYY"

Step 1: Parse Date
┌──────────────────────┐
│ Date: 2024-12-31     │
│ Parse into:          │
│ - year: 2024         │
│ - month: 12          │
│ - day: 31            │
└──────────────────────┘

Step 2: Create Token Map
┌──────────────────────┐
│ Tokens:              │
│ YYYY → 2024          │
│ YY   → 24            │
│ MM   → 12            │
│ M    → 12            │
│ DD   → 31            │
│ D    → 31            │
└──────────────────────┘

Step 3: Replace (Longest First)
┌──────────────────────┐
│ Format: DD/MM/YYYY   │
│ Replace YYYY → 2024  │
│ Result: DD/MM/2024   │
│ Replace MM → 12      │
│ Result: DD/12/2024   │
│ Replace DD → 31      │
│ Result: 31/12/2024   │
└──────────────────────┘

Output: "31/12/2024"
```

---

## Component Integration Pattern

```
┌────────────────────────────────────────────────────────┐
│              Parent Component                          │
│                                                         │
│  import { formatDate } from "../../utils/dateFormatter"│
│  import useUserSettings from "../../hooks/useUserSettings"│
│                                                         │
│  const ParentComponent = () => {                       │
│    const settings = useUserSettings();                 │
│    const bills = [...]; // data                        │
│                                                         │
│    return (                                            │
│      <ChildComponent                                   │
│        bills={bills}                                   │
│        dateFormat={settings.dateFormat}                │
│      />                                                │
│    );                                                  │
│  };                                                    │
└────────────────┬───────────────────────────────────────┘
                 │
                 │ Passes dateFormat as prop
                 ▼
┌────────────────────────────────────────────────────────┐
│              Child Component                           │
│                                                         │
│  import { formatDate } from "../../utils/dateFormatter"│
│                                                         │
│  const ChildComponent = ({                            │
│    bills,                                              │
│    dateFormat = "DD/MM/YYYY"                          │
│  }) => {                                               │
│    return (                                            │
│      <table>                                           │
│        {bills.map(bill => (                           │
│          <tr key={bill.id}>                           │
│            <td>                                        │
│              {formatDate(bill.date, dateFormat)}      │
│            </td>                                       │
│          </tr>                                         │
│        ))}                                             │
│      </table>                                          │
│    );                                                  │
│  };                                                    │
└────────────────────────────────────────────────────────┘
```

---

## Format Addition Workflow

```
New Format Request: "DD.MM.YYYY" (German)

Step 1: Update Settings Configuration
┌─────────────────────────────────────┐
│ settingsConfig.js                   │
│                                     │
│ export const DATE_FORMAT_OPTIONS = [│
│   ...existing formats,              │
│   {                                 │
│     value: "DD.MM.YYYY",           │
│     label: "📅 DD.MM.YYYY (German)"│
│   }                                 │
│ ]                                   │
└─────────────────────────────────────┘
         │
         ▼
Step 2: Update Backend (Optional)
┌─────────────────────────────────────┐
│ Database migration                  │
│ Add "DD.MM.YYYY" to check constraint│
└─────────────────────────────────────┘
         │
         ▼
Step 3: DONE! ✅
┌─────────────────────────────────────┐
│ dateFormatter.js automatically      │
│ handles the new format!             │
│                                     │
│ formatDate("2024-12-31",           │
│            "DD.MM.YYYY")           │
│ → "31.12.2024" ✅                  │
└─────────────────────────────────────┘
```

---

## Error Handling Flow

```
User Input: "invalid-date"

┌────────────────────┐
│ formatDate(        │
│   "invalid-date",  │
│   "DD/MM/YYYY"     │
│ )                  │
└────┬───────────────┘
     │
     ▼
┌────────────────────┐
│ new Date(input)    │
│ → Invalid Date     │
└────┬───────────────┘
     │
     ▼
┌────────────────────┐
│ isNaN(date.getTime())│
│ → true             │
└────┬───────────────┘
     │
     ▼
┌────────────────────┐
│ console.warn()     │
│ Log warning        │
└────┬───────────────┘
     │
     ▼
┌────────────────────┐
│ return ""          │
│ Empty string       │
└────────────────────┘
```

---

## File Structure

```
src/
├── utils/
│   ├── dateFormatter.js                 ← Core utility
│   ├── DATE_FORMATTER_README.md         ← Full documentation
│   ├── DATE_FORMATTER_MIGRATION.md      ← Migration guide
│   ├── DATE_FORMATTER_SUMMARY.md        ← Implementation summary
│   ├── DATE_FORMATTER_ARCHITECTURE.md   ← This file
│   └── dateFormatterExamples.jsx        ← 13 usage examples
│
├── hooks/
│   └── useUserSettings.js               ← Settings hook
│
├── pages/Landingpage/
│   ├── BillReport.jsx                   ← ✅ Updated
│   ├── NewExpense.jsx                   ← ✅ Good
│   ├── EditExpense.jsx                  ← ✅ Good
│   ├── CreateBill.jsx                   ← ✅ Good
│   └── Bills/
│       ├── BillAccordian.jsx           ← ⏳ Pending
│       └── BillSummary.jsx             ← ⏳ Pending
│
└── Settings/
    └── constants/
        └── settingsConfig.js            ← Format definitions
```

---

## Performance Characteristics

```
┌──────────────────────────────────────────────────────┐
│ Operation            │ Time Complexity │ Notes       │
├──────────────────────┼─────────────────┼─────────────┤
│ formatDate()         │ O(1)            │ Constant    │
│ parseDate()          │ O(1)            │ Regex match │
│ isValidDateFormat()  │ O(1)            │ Parse+check │
│ formatRelativeDate() │ O(1)            │ Date math   │
│ getMonthName()       │ O(1)            │ Intl.format │
│ getDayName()         │ O(1)            │ Intl.format │
└──────────────────────────────────────────────────────┘

Memory: ~1KB (no external dependencies)
Bundle Size Impact: Minimal
```

---

## Testing Strategy

```
Unit Tests
├── formatDate()
│   ├── Test all current formats ✓
│   ├── Test with Date objects ✓
│   ├── Test with null/undefined ✓
│   └── Test with invalid dates ✓
│
├── parseDate()
│   ├── Test all current formats ✓
│   ├── Test invalid inputs ✓
│   └── Test edge cases ✓
│
├── formatRelativeDate()
│   ├── Test "Today" ✓
│   ├── Test "Yesterday" ✓
│   ├── Test "2 days ago" ✓
│   └── Test old dates ✓
│
└── Localization
    ├── Test month names ✓
    ├── Test day names ✓
    └── Test different locales ✓

Integration Tests
├── User changes format in settings
├── Date updates throughout app
└── Format persists on reload

E2E Tests
├── Create bill with date
├── Change date format in settings
└── Verify all dates update
```

---

## Future Enhancements Roadmap

```
Version 1.0 (Current)
├── ✅ Basic formatting (DD/MM/YYYY, MM/DD/YYYY, YYYY-MM-DD)
├── ✅ DateTime formatting (12h/24h)
├── ✅ Relative dates (Today, Yesterday)
├── ✅ Parsing and validation
└── ✅ Localized month/day names

Version 1.1 (Planned)
├── ⏳ Week numbers support
├── ⏳ Quarter support (Q1, Q2, Q3, Q4)
├── ⏳ Fiscal year support
└── ⏳ More format tokens (MMM for short month)

Version 1.2 (Future)
├── 📅 Date range formatting
├── 📅 Natural language ("2 weeks ago")
├── 📅 Timezone support
└── 📅 Date arithmetic utilities

Version 2.0 (Long-term)
├── 🔮 AI-powered format detection
├── 🔮 Multi-language format names
├── 🔮 Custom format builder UI
└── 🔮 Format templates library
```

---

## Dependencies

```
┌─────────────────────────────────────┐
│ External Dependencies: NONE ✅       │
├─────────────────────────────────────┤
│ Only uses native JavaScript:        │
│ - Date object                       │
│ - String methods                    │
│ - RegExp                            │
│ - Intl.DateTimeFormat              │
└─────────────────────────────────────┘

Internal Dependencies:
└── useUserSettings hook
    └── Redux userSettings state
        └── Settings configuration
```

---

## Browser Compatibility

```
✅ Chrome 90+
✅ Firefox 88+
✅ Safari 14+
✅ Edge 90+
✅ Opera 76+

Uses:
- ES6+ features (const, let, arrow functions)
- Template literals
- Array methods (map, reduce, forEach)
- Intl.DateTimeFormat (widely supported)
```

---

*Architecture designed for scalability, maintainability, and future enhancements.*
