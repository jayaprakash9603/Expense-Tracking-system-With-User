/**
 * i18n Translations Index
 * Exports all available translations
 * Currently supports: English (en), Hindi (hi), and Telugu (te)
 */
import { en } from "./en";
import { hi } from "./hi";
import { te } from "./te";

export const translations = {
  en,
  hi,
  te,
};
