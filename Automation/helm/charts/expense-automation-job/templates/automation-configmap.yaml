apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.job.name }}-automation-configuration
  labels:
    app.kubernetes.io/name: {{ .Values.job.name }}
data:
  AutomationConfiguration.properties: {{ (tpl (.Files.Get "resources/AutomationConfiguration.properties") .) | quote }}
