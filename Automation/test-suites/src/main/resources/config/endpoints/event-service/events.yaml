endpoints:

  - key: events.create
    method: POST
    path: /api/events
    auth: true

  - key: events.list-user
    method: GET
    path: /api/events/user/{userId}
    auth: true

  - key: events.by-id
    method: GET
    path: /api/events/{eventId}/user/{userId}
    auth: true

  - key: events.update
    method: PUT
    path: /api/events/{eventId}/user/{userId}
    auth: true

  - key: events.delete
    method: DELETE
    path: /api/events/{eventId}/user/{userId}
    auth: true

  - key: events.summary
    method: GET
    path: /api/events/{eventId}/summary/user/{userId}
    auth: true

  - key: events.analytics
    method: GET
    path: /api/events/{eventId}/analytics/user/{userId}
    auth: true
