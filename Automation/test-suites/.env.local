# Local API test credentials — generated for monolith on port 8080
# Source before running:  Get-Content Automation/test-suites/.env.local | ForEach-Object { if ($_ -match '^([^#=]+)=(.*)$') { Set-Item -Path "env:$($matches[1].Trim())" -Value $matches[2].Trim() } }

API_BASE_URL=http://localhost:8080
TEST_USERNAME=auto.api.347332@example.test
TEST_PASSWORD=AutoTest123!
TEST_SIGNUP_PASSWORD=AutoTest123!
JWT_SECRET=expensio-static-jwt-hmac-secret-key-min-32-chars-v1
