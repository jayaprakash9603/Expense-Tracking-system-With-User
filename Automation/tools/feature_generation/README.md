# Feature Generation Tools

Generate domain-aligned feature skeletons for `test-suites`.

Example:

```bash
python tools/feature_generation/generate_feature_template.py expenses "Create expense with valid payload"
```
