# Google OAuth Configuration
# Get your Client ID from Google Cloud Console:
# https://console.cloud.google.com/apis/credentials

REACT_APP_GOOGLE_CLIENT_ID=your-google-client-id-here

# API Configuration
# Set the backend API base URL (use HTTPS in production)
REACT_APP_API_BASE_URL=http://localhost:8080

# For production deployment, use HTTPS:
# REACT_APP_API_BASE_URL=https://api.your-domain.com

# Notification WebSocket URL (optional)
# Defaults to {REACT_APP_API_BASE_URL}/notifications which works for both
# monolithic mode and microservice mode (when Gateway proxies WebSocket).
# Override only if connecting directly to the Notification-Service:
# REACT_APP_NOTIFICATION_WS_URL=http://localhost:6003/notifications
