spring:
  cloud:
    gateway:
      routes:
        - id: USER-SERVICE
          uri: http://host.docker.internal:6001
          predicates:
            - Path=/auth/**, /user/**, /api/user/**, /api/admin/**, /api/roles/**, /api/permissions/**
        - id: BUDGET-SERVICE
          uri: http://host.docker.internal:6005
          predicates:
            - Path= /api/budgets/**
        - id: CHAT-SERVICE
          uri: http://host.docker.internal:7001
          predicates:
            - Path= /api/chats/**
        - id: FRIENDSHIP-SERVICE
          uri: http://host.docker.internal:6009
          predicates:
            - Path= /api/friendships/**,/api/groups/**
        - id: PAYMENT-SERVICE
          uri: http://host.docker.internal:6006
          predicates:
            - Path= /api/payment-methods/**
        - id: CATEGORY-SERVICE
          uri: http://host.docker.internal:6008
          predicates:
            - Path= /api/categories/**
        - id: BILL-SERVICE
          uri: http://host.docker.internal:6007
          predicates:
            - Path= /api/bills/**
        - id: AUDIT-SERVICE
          uri: http://host.docker.internal:6004
          predicates:
            - Path=/api/audit-logs/**, /audit-logs/**
        - id: EXPENSE-TRACKING-SYSTEM
          uri: http://host.docker.internal:6000
          predicates:
            - Path= /api/expenses/**, /api/settings/**, /api/budgets/**, /api/bills/** , /api/categories/**,/daily-summary/**, /api/friendships/**, /api/investment/**,/api/payment-methods/**
        - id: NOTIFICATION-SERVICE
          uri: http://host.docker.internal:6003
          predicates:
            - Path=/api/notifications/**, /notifications/** , /api/notification-preferences/**
      default-filters:
        - name: DedupeResponseHeader
          args:
            name: "Access-Control-Allow-Origin"
            strategy: "RETAIN_FIRST"
        - name: DedupeResponseHeader
          args:
            name: "Access-Control-Allow-Credentials"
            strategy: "RETAIN_FIRST"
      globalcors:
        cors-configurations:
          "[/**]":
            allowedOrigins: "*"
            allowedMethods: "*"
            allowedHeaders: "*"
  datasource:
    url: jdbc:mysql://host.docker.internal:5000/gateway_service
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver

eureka:
  instance:
    prefer-ip-address: true
  client:
    fetch-registry: true
    register-with-eureka: true
    registry-fetch-interval: 5s
    serviceUrl:
      defaultZone: http://eureka-server:8761/eureka
